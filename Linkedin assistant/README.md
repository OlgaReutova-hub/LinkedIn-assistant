# 🤖 LinkedIn Job Assistant

Telegram-бот для работы с LinkedIn через MCP (Model Context Protocol). Бот умеет читать ваш профиль LinkedIn и искать вакансии, используя естественный язык для общения.

## 🎯 Возможности

✅ **Только для чтения** (строго без генерации контента):
- 📋 Просмотр профиля LinkedIn (опыт, образование, навыки)
- 💼 Поиск вакансий на LinkedIn

❌ **НЕ поддерживается** (по дизайну):
- Генерация постов
- Отправка сообщений
- Публикация контента

## 🏗️ Архитектура

```
Telegram User
    ↓
Telegram Bot (python-telegram-bot)
    ↓
Flask Backend (API)
    ↓
OpenAI GPT-4 (классификация намерений)
    ↓
LinkedIn Service → MCP Server → LinkedIn API
```

## 📁 Структура проекта

```
.
├── app/
│   ├── __init__.py
│   ├── config.py              # Конфигурация (ключи, настройки)
│   ├── main.py                # Flask backend
│   ├── telegram_bot.py        # Telegram бот
│   ├── openai_service.py      # OpenAI интеграция
│   └── linkedin_service.py    # LinkedIn MCP интеграция
├── linkedin-mcp-runner/       # MCP сервер (репозиторий)
├── run_bot.py                 # Точка входа для бота
├── Dockerfile                 # Docker образ backend
├── docker-compose.yml         # Оркестрация контейнеров
├── requirements.txt           # Python зависимости
├── .env                       # Секретные ключи (НЕ коммитить!)
├── env.example                # Пример конфигурации
└── README.md                  # Эта документация
```

## 🚀 Быстрый старт

### Предварительные требования

- **Python 3.11+**
- **Node.js 18+** (для MCP сервера)
- **Docker & Docker Compose** (опционально)

### 1️⃣ Клонирование и настройка

```bash
# Перейти в директорию проекта
cd "C:\Users\Asus\Desktop\Курсор\MCP LinkedIn"

# Создать .env файл из примера
copy env.example .env

# Отредактировать .env и вставить свои ключи
notepad .env
```

**Откуда взять ключи:**

- **OPENAI_API_KEY**: https://platform.openai.com/api-keys
- **TELEGRAM_BOT_TOKEN**: создать бота через [@BotFather](https://t.me/BotFather)
- **LINKEDIN_MCP_API_KEY**: https://ligo.ertiqah.com/settings#integrations

### 2️⃣ Локальный запуск (без Docker)

#### Установка зависимостей Python

```bash
pip install -r requirements.txt
```

#### Установка MCP сервера

```bash
cd linkedin-mcp-runner
npm install
cd ..
```

#### Запуск бота

```bash
python run_bot.py
```

#### Запуск Flask backend (в отдельном терминале)

```bash
python app/main.py
```

### 3️⃣ Запуск через Docker

```bash
# Собрать и запустить все сервисы
docker-compose up --build

# Или в фоновом режиме
docker-compose up -d --build

# Проверить логи
docker-compose logs -f

# Остановить
docker-compose down
```

## 🔧 Настройка MCP в Cursor

Для использования MCP сервера в Cursor IDE:

### 1. Создайте/отредактируйте конфигурацию MCP

**Windows:** `%USERPROFILE%\.cursor\mcp.json`  
**Linux/Mac:** `~/.cursor/mcp.json`

### 2. Добавьте конфигурацию LinkedIn MCP

```json
{
  "mcpServers": {
    "linkedin": {
      "command": "npx",
      "args": ["-y", "linkedin-mcp-runner"],
      "env": {
        "LINKEDIN_MCP_API_KEY": "ВАШ_API_КЛЮЧ"
      }
    }
  }
}
```

### 3. Перезапустите Cursor

## 📝 Примеры использования

### В Telegram боте

```
Вы: Привет
Бот: 👋 Привет! Я — LinkedIn Job Assistant...

Вы: Покажи мой профиль
Бот: 📋 Ваш профиль LinkedIn:
     🎯 Senior Python Developer
     💼 Опыт работы:
     • Python Developer в Google (2020-2023)
     ...

Вы: Найди вакансии Python разработчика
Бот: 🔍 Ищу вакансии: Python Developer...
     💼 Найдено вакансий: 15
     1. 🏢 Senior Python Developer
        Компания: Microsoft
        📍 Berlin, Germany
        ...

Вы: Удалённая работа data scientist
Бот: 🔍 Ищу вакансии: Data Scientist (remote)...
```

### Через Flask API

#### Классификация запроса

```bash
curl -X POST http://localhost:5000/api/classify \
  -H "Content-Type: application/json" \
  -d '{"message": "найди вакансии python developer в Берлине"}'
```

Ответ:
```json
{
  "intent": "JOBS",
  "confidence": 0.95,
  "job_params": {
    "role": "Python Developer",
    "location": "Berlin",
    "keywords": null
  }
}
```

#### Получение профиля

```bash
curl http://localhost:5000/api/profile
```

#### Поиск вакансий

```bash
curl -X POST http://localhost:5000/api/jobs/search \
  -H "Content-Type: application/json" \
  -d '{
    "query": "Data Scientist",
    "location": "remote",
    "keywords": ["Python", "ML"]
  }'
```

## 🔒 Безопасность

- ✅ Все ключи хранятся в `.env` (не коммитится в git)
- ✅ Используется `.gitignore` для исключения секретов
- ✅ Docker контейнеры запускаются от непривилегированного пользователя
- ✅ Минимальный набор разрешений для MCP сервера

## 🐛 Отладка

### Проверка здоровья backend

```bash
curl http://localhost:5000/health
```

### Логи Docker

```bash
# Все сервисы
docker-compose logs -f

# Только бот
docker-compose logs -f telegram-bot

# Только backend
docker-compose logs -f backend
```

### Проверка MCP сервера

```bash
cd linkedin-mcp-runner
npx linkedin-mcp-runner setup --api-key YOUR_API_KEY
```

## 📊 Как работает классификация запросов

OpenAI GPT-4 анализирует текст пользователя и определяет намерение (intent):

| Intent | Описание | Примеры запросов |
|--------|----------|------------------|
| `PROFILE` | Запрос о профиле | "мой профиль", "покажи опыт" |
| `JOBS` | Поиск вакансий | "найди вакансии", "работа python developer" |
| `UNKNOWN` | Неизвестный запрос | "привет", "как дела?" |

Для `JOBS` интента извлекаются параметры:
- **role** (обязательно): должность/роль
- **location** (опционально): город/страна/"remote"
- **keywords** (опционально): навыки, технологии

## ⚙️ Переменные окружения

Полный список в `env.example`:

| Переменная | Описание | Обязательна |
|------------|----------|-------------|
| `OPENAI_API_KEY` | API ключ OpenAI | ✅ |
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота | ✅ |
| `LINKEDIN_MCP_API_KEY` | API ключ LinkedIn MCP | ✅ |
| `FLASK_ENV` | Окружение Flask | ❌ (по умолчанию: development) |
| `FLASK_DEBUG` | Режим отладки | ❌ (по умолчанию: 1) |
| `FLASK_HOST` | Хост Flask | ❌ (по умолчанию: 0.0.0.0) |
| `FLASK_PORT` | Порт Flask | ❌ (по умолчанию: 5000) |

## 🛠️ Технологический стек

- **Python 3.11** - основной язык
- **Flask** - backend API
- **python-telegram-bot** - Telegram интеграция
- **OpenAI GPT-4** - NLP и классификация
- **Node.js + MCP** - LinkedIn интеграция
- **Docker** - контейнеризация
- **Pydantic** - валидация данных

## 📚 Дополнительные ресурсы

- [LinkedIn MCP Runner](https://github.com/ertiqah/linkedin-mcp-runner)
- [Model Context Protocol](https://modelcontextprotocol.io/)
- [LiGo Platform](https://ligo.ertiqah.com/)
- [OpenAI API](https://platform.openai.com/docs/)
- [python-telegram-bot Docs](https://docs.python-telegram-bot.org/)

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте логи: `docker-compose logs -f`
2. Убедитесь, что все ключи корректны в `.env`
3. Проверьте, что MCP сервер настроен: `npx linkedin-mcp-runner setup --api-key YOUR_KEY`
4. Проверьте health endpoint: `curl http://localhost:5000/health`

## 📄 Лицензия

MIT License - используйте свободно для личных и коммерческих проектов.

## 🎉 Автор

Создано с ❤️ как демонстрация интеграции Telegram + OpenAI + LinkedIn через MCP

---

**Важно:** Этот бот создан исключительно для чтения данных LinkedIn и поиска вакансий. 
Никакой генерации или публикации контента не поддерживается по дизайну.
